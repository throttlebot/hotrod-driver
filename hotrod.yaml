---
apiVersion: v1
kind: Service
metadata:
  name: hotrod-driver
  labels:
    appName: hotrod-driver
spec:
  # if your cluster supports it, uncomment the following to automatically create
  # an external load-balanced IP for the driver service.
  ports:
  - name: http-hotrod-driver
    port: 8082
  type: ClusterIP
  selector:
    appName: hotrod-driver
---
apiVersion: apps/v1beta2 #  for k8s versions before 1.9.0 use apps/v1beta2  and before 1.8.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: GITLAB_CI_ENV_SLUG-hotrod-driver
spec:
  selector:
    matchLabels:
      appName: hotrod-driver
  replicas: REPLICA_COUNT
  template:
    metadata:
      labels:
        appName: hotrod-driver
        track: APPLICATION_TRACK
        app: GITLAB_CI_ENV_SLUG
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
    spec:
      containers:
      - name: hotrod-driver
        image: registry.hub.docker.com/willwangkelda/hotrod-driver:COMMITID
        command: ["/go/bin/hotrod", "driver"]
        env:
        - name: GET_HOSTS_FROM
          value: dns
        ports:
        - containerPort: 8082
